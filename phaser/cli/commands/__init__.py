# This is annoying thing to have to do. Is there not a more dynamic way to
# import the commands from all modules so that the tests can access them?
from phaser.cli.commands.run import RunPipelineCommand
